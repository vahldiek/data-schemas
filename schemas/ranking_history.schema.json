{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://reprodb.github.io/data-schemas/schemas/ranking_history.schema.json",
  "title": "RankingHistoryEntry Collection",
  "description": "Array of RankingHistoryEntry records. Each element: Dated snapshot of all author rankings, enabling rank-over-time analysis.",
  "version": "0.3.0",
  "type": "array",
  "items": {
    "$ref": "#/$defs/RankingHistoryEntry"
  },
  "$defs": {
    "RankingSnapshot": {
      "additionalProperties": false,
      "description": "A single author's ranking at a point in time.",
      "properties": {
        "rank": {
          "description": "Ranking position at this snapshot date (with ties).",
          "examples": [
            1
          ],
          "minimum": 1,
          "title": "Rank",
          "type": "integer"
        },
        "score": {
          "description": "Combined score at this snapshot: artifact_score + citation_score + ae_score.",
          "examples": [
            15
          ],
          "minimum": 0,
          "title": "Score",
          "type": "integer"
        },
        "as": {
          "description": "Artifact score at this snapshot. Abbreviated 'as' in JSON output.",
          "examples": [
            9
          ],
          "minimum": 0,
          "title": "As",
          "type": "integer"
        },
        "aes": {
          "description": "AE service score: (memberships × 3) + (chairs × 2).",
          "examples": [
            6
          ],
          "minimum": 0,
          "title": "Aes",
          "type": "integer"
        },
        "tp": {
          "description": "Total papers at tracked conferences at this snapshot.",
          "examples": [
            42
          ],
          "minimum": 0,
          "title": "Tp",
          "type": "integer"
        },
        "ta": {
          "description": "Total artifacts authored at this snapshot.",
          "examples": [
            5
          ],
          "minimum": 0,
          "title": "Ta",
          "type": "integer"
        },
        "ar": {
          "description": "Artifact rate: (total_artifacts / total_papers) * 100.",
          "examples": [
            71.4
          ],
          "maximum": 100,
          "minimum": 0,
          "title": "Ar",
          "type": "number"
        },
        "rr": {
          "description": "Reproducibility rate: percentage of artifacts with 'reproduced' badge.",
          "examples": [
            66.7
          ],
          "maximum": 100,
          "minimum": 0,
          "title": "Rr",
          "type": "number"
        }
      },
      "required": [
        "rank",
        "score",
        "as",
        "aes",
        "tp",
        "ta",
        "ar",
        "rr"
      ],
      "title": "RankingSnapshot",
      "type": "object"
    },
    "RankingHistoryEntry": {
      "additionalProperties": false,
      "description": "Dated snapshot of all author rankings, enabling rank-over-time analysis.",
      "properties": {
        "date": {
          "description": "Snapshot date in 'YYYY-MM' format, e.g. '2026-04'.",
          "examples": [
            "2025-01-15"
          ],
          "title": "Date",
          "type": "string"
        },
        "entries": {
          "additionalProperties": {
            "$ref": "#/$defs/RankingSnapshot"
          },
          "description": "Author name (DBLP format) → ranking snapshot. Keys include disambiguation suffixes.",
          "title": "Entries",
          "type": "object"
        }
      },
      "required": [
        "date",
        "entries"
      ],
      "title": "RankingHistoryEntry",
      "type": "object"
    }
  }
}
