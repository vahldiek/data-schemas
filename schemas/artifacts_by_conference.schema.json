{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://reprodb.github.io/data-schemas/schemas/artifacts_by_conference.schema.json",
  "title": "ConferenceEntry Collection",
  "description": "Array of ConferenceEntry records. Each element: Artifact counts and badge breakdowns for a single conference across all tracked years.",
  "version": "0.3.0",
  "type": "array",
  "items": {
    "$ref": "#/$defs/ConferenceEntry"
  },
  "$defs": {
    "YearBreakdown": {
      "additionalProperties": false,
      "description": "Per-year breakdown of artifact counts and badges.",
      "properties": {
        "year": {
          "description": "Publication year, e.g. 2023.",
          "examples": [
            2023
          ],
          "title": "Year",
          "type": "integer"
        },
        "total": {
          "description": "Total number of evaluated artifacts for this conference in this year.",
          "examples": [
            45
          ],
          "minimum": 0,
          "title": "Total",
          "type": "integer"
        },
        "available": {
          "description": "Number of artifacts that received the 'available' badge.",
          "examples": [
            30
          ],
          "minimum": 0,
          "title": "Available",
          "type": "integer"
        },
        "functional": {
          "description": "Number of artifacts that received the 'functional' badge.",
          "examples": [
            25
          ],
          "minimum": 0,
          "title": "Functional",
          "type": "integer"
        },
        "reproducible": {
          "description": "Number of artifacts that received the 'reproduced' badge.",
          "minimum": 0,
          "title": "Reproducible",
          "type": "integer"
        },
        "reusable": {
          "description": "Number of artifacts that received the 'reusable' badge.",
          "examples": [
            5
          ],
          "minimum": 0,
          "title": "Reusable",
          "type": "integer"
        }
      },
      "required": [
        "year",
        "total",
        "available",
        "functional",
        "reproducible",
        "reusable"
      ],
      "title": "YearBreakdown",
      "type": "object"
    },
    "ConferenceEntry": {
      "additionalProperties": false,
      "description": "Artifact counts and badge breakdowns for a single conference across all tracked years.",
      "properties": {
        "name": {
          "description": "Conference abbreviation, e.g. 'ACSAC', 'OSDI', 'USENIXSEC'.",
          "examples": [
            "Mathias Payer"
          ],
          "title": "Name",
          "type": "string"
        },
        "category": {
          "description": "Research domain: 'systems' or 'security'.",
          "enum": [
            "systems",
            "security"
          ],
          "title": "Category",
          "type": "string"
        },
        "venue_type": {
          "description": "Venue type: 'conference' for main conferences, 'workshop' for co-located workshops.",
          "enum": [
            "conference",
            "workshop"
          ],
          "examples": [
            "conference"
          ],
          "title": "Venue Type",
          "type": "string"
        },
        "total_artifacts": {
          "description": "Total number of evaluated artifacts across all years for this conference.",
          "examples": [
            127
          ],
          "minimum": 0,
          "title": "Total Artifacts",
          "type": "integer"
        },
        "years": {
          "description": "Per-year breakdown of artifact counts and badge distributions. Ordered chronologically.",
          "examples": [
            [
              2021,
              2022,
              2023
            ]
          ],
          "items": {
            "$ref": "#/$defs/YearBreakdown"
          },
          "title": "Years",
          "type": "array"
        }
      },
      "required": [
        "name",
        "category",
        "venue_type",
        "total_artifacts",
        "years"
      ],
      "title": "ConferenceEntry",
      "type": "object"
    }
  }
}
