{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://reprodb.github.io/data-schemas/schemas/artifact_availability.schema.json",
  "version": "0.3.0",
  "$defs": {
    "AvailabilityRecord": {
      "additionalProperties": false,
      "description": "Accessibility check result for a single artifact URL.",
      "properties": {
        "conference": {
          "description": "Conference abbreviation, e.g. 'ATC', 'USENIXSEC'.",
          "examples": [
            "OSDI"
          ],
          "title": "Conference",
          "type": "string"
        },
        "year": {
          "description": "Publication year, e.g. 2023.",
          "examples": [
            2023
          ],
          "title": "Year",
          "type": "integer"
        },
        "area": {
          "description": "Research area: 'systems' or 'security'.",
          "examples": [
            "systems"
          ],
          "title": "Area",
          "type": "string"
        },
        "title": {
          "description": "Title of the paper whose artifact URL was checked.",
          "examples": [
            "Understanding and Detecting Software Upgrade Failures in Distributed Systems"
          ],
          "title": "Title",
          "type": "string"
        },
        "url_key": {
          "description": "URL field name from the source data, e.g. 'repository_url', 'artifact_url'.",
          "examples": [
            "repository_url"
          ],
          "title": "Url Key",
          "type": "string"
        },
        "url": {
          "description": "The artifact URL that was checked, e.g. 'https://github.com/org/repo'.",
          "examples": [
            "https://github.com/org/repo"
          ],
          "title": "Url",
          "type": "string"
        },
        "platform": {
          "description": "Detected hosting platform: 'GitHub', 'GitLab', 'Zenodo', 'Figshare', 'Bitbucket', or 'DOI-other'.",
          "examples": [
            "github.com"
          ],
          "title": "Platform",
          "type": "string"
        },
        "accessible": {
          "description": "True if the URL returned HTTP 200 at check time, false otherwise.",
          "examples": [
            true
          ],
          "title": "Accessible",
          "type": "boolean"
        }
      },
      "required": [
        "conference",
        "year",
        "area",
        "title",
        "url_key",
        "url",
        "platform",
        "accessible"
      ],
      "title": "AvailabilityRecord",
      "type": "object"
    },
    "AvailabilitySummary": {
      "additionalProperties": false,
      "description": "Aggregate summary of artifact URL accessibility.",
      "properties": {
        "checked_at": {
          "description": "UTC timestamp when the availability check was run, e.g. '2026-04-27 21:27:11 UTC'.",
          "examples": [
            "2026-04-01T12:00:00Z"
          ],
          "title": "Checked At",
          "type": "string"
        },
        "total_urls": {
          "description": "Total number of artifact URLs checked across all platforms.",
          "examples": [
            2831
          ],
          "minimum": 0,
          "title": "Total Urls",
          "type": "integer"
        },
        "accessible_urls": {
          "description": "Number of URLs that were accessible (HTTP 200).",
          "examples": [
            2450
          ],
          "minimum": 0,
          "title": "Accessible Urls",
          "type": "integer"
        },
        "accessibility_pct": {
          "description": "Overall accessibility percentage: (accessible_urls / total_urls) * 100.",
          "examples": [
            86.7
          ],
          "maximum": 100,
          "minimum": 0,
          "title": "Accessibility Pct",
          "type": "number"
        },
        "by_platform": {
          "additionalProperties": {
            "$ref": "#/$defs/PlatformStats"
          },
          "description": "Breakdown by hosting platform. Keys are platform names: 'GitHub', 'GitLab', 'Zenodo', 'Figshare', 'Bitbucket', 'DOI-other'.",
          "title": "By Platform",
          "type": "object"
        },
        "by_area": {
          "additionalProperties": {
            "$ref": "#/$defs/PlatformStats"
          },
          "description": "Breakdown by research area. Keys: 'systems', 'security'.",
          "title": "By Area",
          "type": "object"
        },
        "by_year": {
          "additionalProperties": {
            "$ref": "#/$defs/PlatformStats"
          },
          "description": "Breakdown by publication year. Keys are year strings, e.g. '2020', '2023'.",
          "title": "By Year",
          "type": "object"
        },
        "by_year_area": {
          "additionalProperties": {
            "additionalProperties": {
              "$ref": "#/$defs/PlatformStats"
            },
            "type": "object"
          },
          "description": "Nested breakdown: year string → area ('systems'/'security') → PlatformStats.",
          "title": "By Year Area",
          "type": "object"
        },
        "by_year_platform": {
          "additionalProperties": {
            "additionalProperties": {
              "$ref": "#/$defs/PlatformStats"
            },
            "type": "object"
          },
          "description": "Nested breakdown: year string → platform name → PlatformStats.",
          "title": "By Year Platform",
          "type": "object"
        },
        "by_conference": {
          "additionalProperties": {
            "$ref": "#/$defs/PlatformStats"
          },
          "description": "Breakdown by conference. Keys are abbreviations, e.g. 'OSDI', 'USENIXSEC', 'NDSS'.",
          "title": "By Conference",
          "type": "object"
        }
      },
      "required": [
        "checked_at",
        "total_urls",
        "accessible_urls",
        "accessibility_pct",
        "by_platform",
        "by_area",
        "by_year",
        "by_year_area",
        "by_year_platform",
        "by_conference"
      ],
      "title": "AvailabilitySummary",
      "type": "object"
    },
    "PlatformStats": {
      "additionalProperties": false,
      "description": "Accessibility stats for a single platform.",
      "properties": {
        "total": {
          "description": "Total number of artifact URLs checked on this platform.",
          "examples": [
            45
          ],
          "minimum": 0,
          "title": "Total",
          "type": "integer"
        },
        "accessible": {
          "description": "Number of URLs that returned HTTP 200 (accessible).",
          "examples": [
            true
          ],
          "minimum": 0,
          "title": "Accessible",
          "type": "integer"
        },
        "pct": {
          "description": "Accessibility percentage: (accessible / total) * 100.",
          "examples": [
            87.5
          ],
          "maximum": 100,
          "minimum": 0,
          "title": "Pct",
          "type": "number"
        }
      },
      "required": [
        "total",
        "accessible",
        "pct"
      ],
      "title": "PlatformStats",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Artifact URL accessibility report: aggregate summary and per-URL check results.",
  "properties": {
    "summary": {
      "$ref": "#/$defs/AvailabilitySummary",
      "description": "Aggregate accessibility summary with breakdowns by platform, area, year, and conference."
    },
    "records": {
      "description": "Individual check results, one record per artifact URL tested.",
      "items": {
        "$ref": "#/$defs/AvailabilityRecord"
      },
      "title": "Records",
      "type": "array"
    }
  },
  "required": [
    "summary",
    "records"
  ],
  "title": "ArtifactAvailability",
  "type": "object"
}
