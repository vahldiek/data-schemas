{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://reprodb.github.io/data-schemas/schemas/artifacts_by_year.schema.json",
  "title": "ArtifactsByYear Collection",
  "description": "Array of ArtifactsByYear records. Each element: Total artifact count for a single year, split into systems and security conferences.",
  "version": "0.3.0",
  "type": "array",
  "items": {
    "$ref": "#/$defs/ArtifactsByYear"
  },
  "$defs": {
    "ArtifactsByYear": {
      "additionalProperties": false,
      "description": "Total artifact count for a single year, split into systems and security conferences.",
      "properties": {
        "year": {
          "description": "Publication year, e.g. 2023.",
          "examples": [
            2023
          ],
          "title": "Year",
          "type": "integer"
        },
        "count": {
          "description": "Total evaluated artifacts across all conferences for this year.",
          "examples": [
            127
          ],
          "minimum": 0,
          "title": "Count",
          "type": "integer"
        },
        "systems": {
          "description": "Number of artifacts from systems conferences (ATC, EUROSYS, FAST, OSDI, SC, SOSP).",
          "examples": [
            75
          ],
          "minimum": 0,
          "title": "Systems",
          "type": "integer"
        },
        "security": {
          "description": "Number of artifacts from security conferences (ACSAC, CHES, NDSS, PETS, SYSTEX, USENIXSEC, WOOT).",
          "examples": [
            52
          ],
          "minimum": 0,
          "title": "Security",
          "type": "integer"
        }
      },
      "required": [
        "year",
        "count",
        "systems",
        "security"
      ],
      "title": "ArtifactsByYear",
      "type": "object"
    }
  }
}
